# Frontend

The frontend is intentionally not implemented during project initialization.

Its first version will focus on a small, recruiter-friendly flow: choose a project, follow the diagnose-to-decision loop, review hypotheses, approve guarded experiments, monitor objective/guardrail/budget status, explore the experiment DAG, and read the evidence-backed final report. A framework will be selected when the agent's initial API workflow is stable.
